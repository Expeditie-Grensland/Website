mixin innerImage(expeditie)
    .img-bg.embed-responsive-item(style=`background-image:url(${getFileUrl(expeditie.backgroundFile)})`)
    .img-inner.embed-responsive-item
        span.h1= expeditie.name
        span.h2= expeditie.subtitle
